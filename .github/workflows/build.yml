name: build

on:
  push:
    branches-ignore:
      - deploy
  pull_request:
    branches-ignore:
      - deploy

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - '3.10'
          - '3.14'
    steps:
    - uses: actions/checkout@v7
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true
    - name: Fetch branches
      run: |
        git remote set-branches --add origin deploy
        git fetch --depth=5 origin master deploy
    - uses: actions/setup-python@v7
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Python dependencies
      run: uv sync --locked --dev --group ci
    - name: Lint code
      run: uv run ruff check
    - name: Pytest
      run: |
        uv run python3 -W default::Warning -m pytest \
          --cov=chatcommands \
          --cov=chatcommunicate \
          --cov=datahandling \
          --cov=findspam \
          --cov=globalvars \
          --cov=helpers \
          --cov=parsing \
          --cov=spamhandling \
          -n 3 \
          --dist loadgroup \
          --timeout=300 \
          --durations=10 \
          test
    - name: Coverage report
      run: |
        echo -e "## Coverage Report\n" >> "$GITHUB_STEP_SUMMARY"
        uv run coverage report --format=markdown -m >> "$GITHUB_STEP_SUMMARY"

  release:
    if: ${{ github.ref == 'refs/heads/master' }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v7
    - name: Push to deploy
      continue-on-error: true
      run: |
        git push origin HEAD:deploy
