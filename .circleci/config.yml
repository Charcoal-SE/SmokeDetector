version: 2
jobs:
  lint:
    docker:
      - image: circleci/python:3.6-stretch
    steps:
      - checkout
      - run:
          name: Install Python dependencies in venv
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip3 install 'flake8==3.4.1'
      - run: flake8 --config=tox_tests.ini ./test/
      - run: flake8 --config=tox_classes.ini ./classes/
      - run: flake8 ./
  test:
    docker:
      - image: circleci/python:3.6-stretch
    steps:
      - checkout
      - run:
          name: Install Python dependencies in venv
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip3 install -r requirements.txt --upgrade
            pip3 install -r user_requirements.txt --upgrade
      - run: python3 -W default::Warning -m pytest test

workflows:
  version: 2
  lint_and_test:
    jobs:
      - lint
      - test
